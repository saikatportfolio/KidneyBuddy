import 'package:flutter/material.dart';

class YourMealsScreen extends StatelessWidget {
  const YourMealsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Your Meals'),
      ),
      body: const Center(
        child: Text('Diet Management Screen - Functionality to be added later'),
      ),
    );
  }
}
